import { ScrollContainer } from '../../styles';
import ClipList from '../contents/ClipList';

const ForkClipTab = () => {
  return (
    <ScrollContainer>
      <ClipList />
    </ScrollContainer>
  );
};

export default ForkClipTab;
