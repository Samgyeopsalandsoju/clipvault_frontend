import ClipList from '../contents/ClipList';
import { ScrollContainer } from '../../styles';

const MyClipTab = () => {
  return (
    <ScrollContainer>
      <ClipList />
    </ScrollContainer>
  );
};

export default MyClipTab;
