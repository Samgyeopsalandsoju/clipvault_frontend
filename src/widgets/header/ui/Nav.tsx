import { SheetContent, SheetHeader, SheetTitle } from '@/shared/ui/sheet';

function NavSheet() {
  return (
    <SheetContent>
      <SheetHeader>
        <SheetTitle>clipVault</SheetTitle>
      </SheetHeader>
    </SheetContent>
  );
}

export { NavSheet };
